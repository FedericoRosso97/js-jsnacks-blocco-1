
const numeri=[];
for(i=0;i<10;i++){
    let Numero=parseInt(prompt('inserisci un numero!'));
    numeri.push(Numero);
     Numero=numeri[0] + numeri[1];
    numeri.push(Numero);
    console.log(numeri);
}
